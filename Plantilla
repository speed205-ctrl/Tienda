< !DOCTYPE html><html lang="es"><head><meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1" /><title>TecnoCell — Accesorios de PC y Teléfono</title><link rel="stylesheet" href="https://cdn.jsdelivr.net/fontsource/css/inter@latest/400.css"><link rel="stylesheet" href="https://cdn.jsdelivr.net/fontsource/css/inter@latest/500.css"><link rel="stylesheet" href="https://cdn.jsdelivr.net/fontsource/css/inter@latest/600.css"><link rel="stylesheet" href="https://cdn.jsdelivr.net/fontsource/css/inter@latest/700.css"><style>:root {
    --bg: #fbfbfd;
    --panel: #f5f5f7;
    --ink: #1d1d1f;
    --mut: #6e6e73;
    --line: rgba(0, 0, 0, .08);
    --blue: #0071e3;
    --blue-h: #0077ed;
    --link: #06c;
    --link-d: #2997ff;
    --wa: #25d366;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box
}

html {
    scroll-behavior: smooth
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Helvetica Neue', sans-serif;
    background: var(--bg);
    color: var(--ink);
    font-size: 15px;
    line-height: 1.5;
    -webkit-font-smoothing: antialiased;
    overflow-x: hidden
}

img {
    display: block;
    max-width: 100%
}

button {
    font-family: inherit;
    cursor: pointer;
    border: none;
    background: none
}

a {
    text-decoration: none;
    color: inherit
}

.wrap {
    max-width: 1080px;
    margin: 0 auto;
    padding: 0 22px
}

::selection {
    background: rgba(0, 113, 227, .2)
}

/* ══ NAV ══ */
.nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 60;
    background: rgba(251, 251, 253, .8);
    backdrop-filter: saturate(180%) blur(20px);
    -webkit-backdrop-filter: saturate(180%) blur(20px);
    border-bottom: 1px solid var(--line)
}

.navIn {
    max-width: 1080px;
    margin: 0 auto;
    padding: 0 22px;
    height: 52px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 18px
}

.logo {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 600;
    font-size: 16px;
    letter-spacing: -.02em
}

.logo svg {
    width: 17px;
    height: 17px;
    fill: var(--ink)
}

.navLinks {
    display: flex;
    gap: 30px
}

.navLinks a {
    font-size: 12.5px;
    font-weight: 500;
    color: rgba(29, 29, 31, .85);
    transition: color .2s
}

.navLinks a:hover {
    color: var(--ink)
}

.bagBtn {
    position: relative;
    display: grid;
    place-items: center;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    transition: background .2s
}

.bagBtn:hover {
    background: rgba(0, 0, 0, .05)
}

.bagBtn svg {
    width: 18px;
    height: 18px;
    stroke: var(--ink);
    fill: none;
    stroke-width: 1.8;
    stroke-linecap: round;
    stroke-linejoin: round
}

.bagN {
    position: absolute;
    top: 1px;
    right: 0;
    min-width: 16px;
    height: 16px;
    padding: 0 4px;
    border-radius: 99px;
    background: var(--blue);
    color: #fff;
    font-size: 10px;
    font-weight: 600;
    display: grid;
    place-items: center
}

.bagN.pop {
    animation: pop .35s
}

@keyframes pop {
    45% {
        transform: scale(1.35)
    }
}

/* ══ HERO ══ */
.hero {
    padding: 120px 0 0;
    text-align: center
}

.eyebrow {
    font-size: 13px;
    font-weight: 600;
    color: var(--blue);
    letter-spacing: .02em;
    margin-bottom: 10px
}

.hero h1 {
    font-size: clamp(42px, 6.5vw, 72px);
    font-weight: 600;
    letter-spacing: -.035em;
    line-height: 1.05
}

.hero .sub {
    font-size: clamp(17px, 2.4vw, 24px);
    font-weight: 500;
    color: var(--mut);
    letter-spacing: -.01em;
    margin: 16px auto 26px;
    max-width: 640px
}

.ctaRow {
    display: flex;
    gap: 22px;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap
}

.btnP {
    background: var(--blue);
    color: #fff;
    border-radius: 999px;
    padding: 12px 26px;
    font-size: 15px;
    font-weight: 500;
    transition: background .2s;
    display: inline-block
}

.btnP:hover {
    background: var(--blue-h)
}

.linkA {
    color: var(--link);
    font-size: 15px;
    font-weight: 500;
    display: inline-flex;
    align-items: center;
    gap: 5px
}

.linkA:hover {
    text-decoration: underline
}

.linkA .chev {
    transition: transform .2s
}

.linkA:hover .chev {
    transform: translateX(3px)
}

/* Banner a sangre completa, sin esquinas */
.heroImg {
    width: 100%;
    margin: 56px 0 0;
    overflow: hidden
}

.heroImg img {
    width: 100%;
    height: clamp(320px, 52vw, 560px);
    object-fit: cover
}

/* ══ BANNERS (full-width, sin bordes redondeados) ══ */
.tiles {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin: 12px 0 0;
    width: 100%
}

.tile {
    width: 100%;
    padding: 72px 22px 0;
    text-align: center;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center
}

.tile.dark {
    background: #000;
    color: #f5f5f7
}

.tile.light {
    background: var(--panel)
}

.tile .eyebrow {
    margin-bottom: 8px
}

.tile.dark .eyebrow {
    color: #f563ff
}

.tile.light .eyebrow {
    color: #b64400
}

.tile h3 {
    font-size: clamp(26px, 3.4vw, 40px);
    font-weight: 600;
    letter-spacing: -.02em;
    line-height: 1.1
}

.tile p {
    font-size: 15px;
    margin: 10px 0 16px
}

.tile.dark p {
    color: #a1a1a6
}

.tile.light p {
    color: var(--mut)
}

.tile.dark .linkA {
    color: var(--link-d)
}

.tile img {
    margin-top: 38px;
    width: 100%;
    height: clamp(260px, 38vw, 440px);
    object-fit: cover
}

/* ══ CATÁLOGO ══ */
.catalog {
    padding: 90px 0 30px;
    text-align: center
}

.catalog h2 {
    font-size: clamp(32px, 4.5vw, 52px);
    font-weight: 600;
    letter-spacing: -.03em
}

.catalog .h2sub {
    color: var(--mut);
    font-size: 16px;
    margin-top: 8px
}

.seg {
    display: inline-flex;
    background: var(--panel);
    border-radius: 999px;
    padding: 4px;
    gap: 2px;
    margin: 30px auto 18px;
    flex-wrap: wrap;
    justify-content: center
}

.seg button {
    padding: 9px 20px;
    border-radius: 999px;
    font-size: 13.5px;
    font-weight: 500;
    color: var(--mut);
    transition: .25s
}

.seg button.active {
    background: #fff;
    color: var(--ink);
    box-shadow: 0 2px 10px rgba(0, 0, 0, .12)
}

.searchW {
    max-width: 440px;
    margin: 0 auto 40px;
    position: relative
}

.searchW svg {
    position: absolute;
    left: 14px;
    top: 50%;
    transform: translateY(-50%);
    width: 15px;
    height: 15px;
    stroke: var(--mut);
    fill: none;
    stroke-width: 2;
    stroke-linecap: round
}

#search {
    width: 100%;
    background: var(--panel);
    border: none;
    border-radius: 14px;
    padding: 12px 16px 12px 40px;
    font: 500 14px 'Inter';
    color: var(--ink);
    outline: none;
    transition: box-shadow .2s
}

#search:focus {
    box-shadow: 0 0 0 3px rgba(0, 113, 227, .35)
}

.grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 20px;
    text-align: center
}

.card {
    background: #fff;
    border-radius: 18px;
    padding: 18px 18px 22px;
    border: 1px solid rgba(0, 0, 0, .04);
    transition: transform .3s, box-shadow .3s;
    animation: up .5s ease both;
    display: flex;
    flex-direction: column
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 16px 40px rgba(0, 0, 0, .09)
}

@keyframes up {
    from {
        opacity: 0;
        transform: translateY(18px)
    }
}

.card .ph {
    background: var(--panel);
    border-radius: 12px;
    aspect-ratio: 1;
    overflow: hidden;
    margin-bottom: 16px
}

.card .ph img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform .5s
}

.card:hover .ph img {
    transform: scale(1.05)
}

.card .cat {
    font-size: 11.5px;
    font-weight: 600;
    color: var(--mut);
    text-transform: uppercase;
    letter-spacing: .06em
}

.card h3 {
    font-size: 16.5px;
    font-weight: 600;
    letter-spacing: -.01em;
    margin: 5px 0 3px
}

.card .desc {
    font-size: 13px;
    color: var(--mut);
    flex: 1
}

.card .price {
    font-size: 15px;
    font-weight: 500;
    color: var(--ink);
    margin: 12px 0
}

.card .price span {
    color: var(--mut);
    font-size: 12.5px
}

.addB {
    background: var(--blue);
    color: #fff;
    border-radius: 999px;
    padding: 9px 0;
    width: 100%;
    font-size: 14px;
    font-weight: 500;
    transition: background .2s
}

.addB:hover {
    background: var(--blue-h)
}

.empty {
    grid-column: 1/-1;
    color: var(--mut);
    padding: 60px 0;
    font-weight: 500
}

/* ══ PASOS ══ */
.how {
    background: var(--panel);
    margin-top: 80px;
    padding: 80px 0
}

.how h2 {
    text-align: center;
    font-size: clamp(28px, 4vw, 44px);
    font-weight: 600;
    letter-spacing: -.03em
}

.how .h2sub {
    text-align: center;
    color: var(--mut);
    margin: 8px 0 50px
}

.stepsG {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
    max-width: 900px;
    margin: 0 auto;
    padding: 0 22px
}

.step {
    text-align: center
}

.step .n {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: #fff;
    color: var(--blue);
    font-weight: 600;
    font-size: 17px;
    display: grid;
    place-items: center;
    margin: 0 auto 16px;
    box-shadow: 0 4px 14px rgba(0, 0, 0, .06)
}

.step h3 {
    font-size: 17px;
    font-weight: 600;
    margin-bottom: 8px;
    letter-spacing: -.01em
}

.step p {
    font-size: 13.5px;
    color: var(--mut)
}

/* ══ FOOTER ══ */
footer {
    background: var(--panel);
    border-top: 1px solid var(--line);
    padding: 40px 0 30px;
    font-size: 12px;
    color: var(--mut)
}

.footG {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr;
    gap: 36px;
    padding-bottom: 30px;
    border-bottom: 1px solid var(--line)
}

.footG h4 {
    font-size: 12px;
    font-weight: 600;
    color: var(--ink);
    margin-bottom: 12px
}

.footG p,
.footG a {
    font-size: 12.5px;
    color: var(--mut);
    line-height: 1.9
}

.footG a:hover {
    color: var(--ink);
    text-decoration: underline
}

.footG .num {
    font-size: 16px;
    font-weight: 600;
    color: var(--ink)
}

.copy {
    padding-top: 18px;
    display: flex;
    justify-content: space-between;
    gap: 14px;
    flex-wrap: wrap
}

/* ══ BOLSA (drawer) ══ */
.overlay {
    position: fixed;
    inset: 0;
    background: rgba(29, 29, 31, .4);
    opacity: 0;
    pointer-events: none;
    transition: .35s;
    z-index: 80;
    backdrop-filter: blur(4px)
}

.overlay.show {
    opacity: 1;
    pointer-events: auto
}

.drawer {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    width: min(440px, 100%);
    background: #fff;
    z-index: 90;
    display: flex;
    flex-direction: column;
    transform: translateX(105%);
    transition: .45s cubic-bezier(.32, .72, 0, 1);
    box-shadow: -20px 0 60px rgba(0, 0, 0, .15)
}

.drawer.show {
    transform: none
}

.dHead {
    padding: 26px 26px 14px;
    display: flex;
    justify-content: space-between;
    align-items: center
}

.dHead h3 {
    font-size: 22px;
    font-weight: 600;
    letter-spacing: -.02em
}

.dHead button {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    color: var(--mut);
    transition: background .2s
}

.dHead button:hover {
    background: var(--panel);
    color: var(--ink)
}

.dBody {
    flex: 1;
    overflow-y: auto;
    padding: 6px 26px;
    display: flex;
    flex-direction: column;
    gap: 20px
}

.cItem {
    display: flex;
    gap: 14px;
    animation: up .3s ease
}

.cItem img {
    width: 76px;
    height: 76px;
    object-fit: cover;
    border-radius: 12px;
    background: var(--panel)
}

.cInfo {
    flex: 1;
    min-width: 0
}

.cInfo h4 {
    font-size: 14px;
    font-weight: 600;
    letter-spacing: -.01em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis
}

.cInfo .cp {
    font-size: 13px;
    color: var(--mut);
    margin: 2px 0 10px
}

.stepper {
    display: inline-flex;
    align-items: center;
    gap: 14px;
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 4px 12px
}

.stepper button {
    font-size: 15px;
    color: var(--mut);
    width: 16px
}

.stepper button:hover {
    color: var(--blue)
}

.stepper span {
    min-width: 14px;
    text-align: center;
    font-size: 13.5px;
    font-weight: 600
}

.cRight {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    align-items: end
}

.cRight strong {
    font-size: 14px;
    font-weight: 600
}

.del {
    color: var(--link);
    font-size: 12px
}

.del:hover {
    text-decoration: underline
}

.dEmpty {
    margin: auto;
    text-align: center;
    color: var(--mut);
    display: flex;
    flex-direction: column;
    gap: 16px;
    align-items: center
}

.dEmpty .ico {
    font-size: 44px;
    opacity: .5
}

.dFoot {
    border-top: 1px solid var(--line);
    padding: 20px 26px 24px
}

.tRow {
    display: flex;
    justify-content: space-between;
    font-size: 14px;
    color: var(--mut);
    margin-bottom: 6px
}

.tRow strong {
    color: var(--ink);
    font-size: 17px;
    font-weight: 600
}

.dFoot .note {
    font-size: 12px;
    color: var(--mut);
    margin-bottom: 16px
}

.waBtn {
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    background: var(--ink);
    color: #fff;
    border-radius: 999px;
    padding: 14px;
    font-size: 15px;
    font-weight: 500;
    transition: background .2s
}

.waBtn:hover {
    background: #000
}

.waBtn svg {
    width: 18px;
    height: 18px;
    fill: var(--wa)
}

.waFloat {
    position: fixed;
    bottom: 24px;
    right: 24px;
    z-index: 70;
    width: 54px;
    height: 54px;
    border-radius: 50%;
    background: var(--wa);
    display: grid;
    place-items: center;
    box-shadow: 0 10px 26px rgba(37, 211, 102, .4);
    transition: transform .2s
}

.waFloat:hover {
    transform: scale(1.08)
}

.waFloat svg {
    width: 26px;
    height: 26px;
    fill: #fff
}

.toast {
    position: fixed;
    bottom: 28px;
    left: 50%;
    transform: translate(-50%, 80px);
    z-index: 120;
    background: rgba(29, 29, 31, .92);
    color: #fff;
    backdrop-filter: blur(10px);
    border-radius: 999px;
    padding: 11px 22px;
    font-size: 13.5px;
    font-weight: 500;
    transition: .35s
}

.toast.show {
    transform: translate(-50%, 0)
}

.rv {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity .8s ease, transform .8s ease
}

.rv.on {
    opacity: 1;
    transform: none
}

@media(max-width:860px) {
    .navLinks {
        display: none
    }

    .stepsG {
        grid-template-columns: 1fr;
        gap: 34px
    }

    .footG {
        grid-template-columns: 1fr
    }

    .hero {
        padding-top: 100px
    }
}

</style></head><body><nav class="nav"><div class="navIn"><a class="logo" href="#"><svg viewBox="0 0 24 24"><path d="M13 2 3 14h7l-1 8 10-12h-7l1-8z" /></svg>TecnoCell</a><div class="navLinks"><a href="#productos">Catálogo</a><a href="#destacados">Destacados</a><a href="#como">Cómo comprar</a><a href="#contacto">Contacto</a></div><button class="bagBtn" id="openCart" aria-label="Abrir bolsa"><svg viewBox="0 0 24 24"><path d="M6 7h12l1 14H5L6 7z" /><path d="M9 7a3 3 0 0 1 6 0" /></svg><span class="bagN" id="cartCount">0</span></button></div></nav><section class="hero"><div class="wrap"><p class="eyebrow">Nuevo catálogo 2026</p><h1>Potencia tu mundo.</h1><p class="sub">Accesorios de PC y teléfono con la calidad que mereces. Pide por WhatsApp y recíbelo en toda Venezuela.</p><div class="ctaRow"><a class="btnP" href="#productos">Comprar ahora</a><a class="linkA" href="https://wa.me/584121585370?text=%C2%A1Hola%20TecnoCell!%20Quiero%20informaci%C3%B3n." target="_blank">Hablar por WhatsApp <span class="chev">›</span></a></div></div><div class="heroImg rv"><img src="https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/1b84cadb5-c153-43f5-830d-36e19be7e3a6.png" alt="Accesorios TecnoCell"></div></section><section class="tiles" id="destacados"><div class="tile dark rv"><p class="eyebrow">Setup gamer</p><h3>Teclado Mecánico RGB.</h3><p>Precisión que se siente en cada tecla.</p><a class="linkA" href="#productos" onclick="goCat('pc')">Comprar <span class="chev">›</span></a><img src="https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/196a9ff00-34b6-45ad-84c8-142bfa32bc91.png" alt="Teclado mecánico"></div><div class="tile light rv"><p class="eyebrow">Carga rápida</p><h3>25W de poder. USB‑C.</h3><p>Tu teléfono al 100% en menos tiempo.</p><a class="linkA" href="#productos" onclick="goCat('tlf')">Comprar <span class="chev">›</span></a><img src="https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/1f1634d8e-9c80-463e-b685-361b7114b879.png" alt="Cargador rápido"></div></section><section class="catalog" id="productos"><div class="wrap"><h2 class="rv">Catálogo.</h2><p class="h2sub rv">Todo lo que tu equipo necesita,
en un solo lugar.</p><div class="seg rv" id="chips"><button class="active" data-cat="all">Todos</button><button data-cat="pc">PC</button><button data-cat="tlf">Teléfono</button><button data-cat="belleza">Belleza</button></div><div class="searchW rv"><svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7" /><path d="m20 20-3.5-3.5" /></svg><input id="search" type="search" placeholder="Buscar en TecnoCell" autocomplete="off"></div><div class="grid" id="grid"></div></div></section><section class="how" id="como"><h2 class="rv">Comprar es fácil.</h2><p class="h2sub rv">Tres pasos y listo.</p><div class="stepsG rv"><div class="step"><div class="n">1</div><h3>Explora</h3><p>Navega el catálogo y añade tus productos favoritos a la bolsa.</p></div><div class="step"><div class="n">2</div><h3>Revisa</h3><p>Ajusta cantidades y verifica el total de tu pedido en tiempo real.</p></div><div class="step"><div class="n">3</div><h3>Pide por WhatsApp</h3><p>Envía tu pedido armado al+58 412‑1585370 y coordina pago y envío.</p></div></div></section><footer id="contacto"><div class="wrap"><div class="footG"><div><h4>TecnoCell</h4><p>Tienda de accesorios de PC,
teléfono y belleza. Atención personalizada por WhatsApp y envíos a toda Venezuela.</p><p class="num" style="margin-top:10px">+58 412‑1585370</p><p><a href="https://wa.me/584121585370" target="_blank">Abrir chat ›</a></p></div><div><h4>Catálogo</h4><p><a href="#productos" onclick="goCat('pc')">Productos de PC</a></p><p><a href="#productos" onclick="goCat('tlf')">Accesorios de teléfono</a></p><p><a href="#productos" onclick="goCat('belleza')">Belleza y skincare</a></p></div><div><h4>Información</h4><p>Envíos a toda Venezuela (24–72 h).</p><p>Pago Móvil · USD · Zelle.</p><p>Garantía en todos los productos.</p><p>Lun – Sáb · 9:00 am – 7:00 pm.</p></div></div><div class="copy"><span>Copyright © 2026 TecnoCell. Todos los derechos reservados.</span><span>Venezuela 🇻</span></div></div></footer><div class="overlay" id="overlay"></div><aside class="drawer" id="drawer"><div class="dHead"><h3>Bolsa</h3><button id="closeCart" aria-label="Cerrar">✕</button></div><div class="dBody" id="cartItems"></div><div class="dFoot"><div class="tRow"><span>Subtotal</span><strong id="cartTotal">$0.00</strong></div><p class="note">El pago y el envío se coordinan por WhatsApp.</p><button class="waBtn" id="checkout"><svg viewBox="0 0 24 24"><path d="M12 2a10 10 0 0 0-8.6 15.1L2 22l5.1-1.3A10 10 0 1 0 12 2zm5.4 14.1c-.2.7-1.3 1.3-1.9 1.4-.5.1-1.1.1-1.8-.1-.4-.1-1-.3-1.7-.6-3-1.3-4.9-4.3-5.1-4.5-.1-.2-1.2-1.6-1.2-3s.7-2.1 1-2.4c.2-.3.5-.3.7-.3h.5c.2 0 .4 0 .6.4.2.5.8 1.9.8 2 .1.1.1.3 0 .5-.3.6-.7.9-.5 1.2.7 1.2 1.6 2 2.8 2.6.3.2.5.1.7-.1.2-.2.8-.9 1-1.2.2-.3.4-.2.7-.1.3.1 1.7.8 2 1 .3.1.5.2.5.3.1.2.1.6-.1 1z" /></svg>Enviar pedido por WhatsApp </button></div></aside><a class="waFloat" href="https://wa.me/584121585370?text=%C2%A1Hola!%20Vengo%20de%20la%20web%20de%20TecnoCell." target="_blank" aria-label="WhatsApp"><svg viewBox="0 0 24 24"><path d="M12 2a10 10 0 0 0-8.6 15.1L2 22l5.1-1.3A10 10 0 1 0 12 2zm5.4 14.1c-.2.7-1.3 1.3-1.9 1.4-.5.1-1.1.1-1.8-.1-.4-.1-1-.3-1.7-.6-3-1.3-4.9-4.3-5.1-4.5-.1-.2-1.2-1.6-1.2-3s.7-2.1 1-2.4c.2-.3.5-.3.7-.3h.5c.2 0 .4 0 .6.4.2.5.8 1.9.8 2 .1.1.1.3 0 .5-.3.6-.7.9-.5 1.2.7 1.2 1.6 2 2.8 2.6.3.2.5.1.7-.1.2-.2.8-.9 1-1.2.2-.3.4-.2.7-.1.3.1 1.7.8 2 1 .3.1.5.2.5.3.1.2.1.6-.1 1z" /></svg></a><div class="toast" id="toast">Añadido a la bolsa</div><script>const WHATS='584121585370';

const CATLABEL= {
    pc: 'PC', tlf:'Teléfono', belleza:'Belleza'
}

;

const PRODUCTS=[ {
    id: 1, cat:'pc', price:25, name:'Mouse Gamer RGB X7', desc:'12.000 DPI y 7 botones programables.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/1f53228d6-250e-46ac-9ea2-14de26ec8199.png'
}

,
{
id: 2, cat:'pc', price:48, name:'Teclado Mecánico RGB K550', desc:'Switches mecánicos y rueda de volumen.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/196a9ff00-34b6-45ad-84c8-142bfa32bc91.png'
}

,
{
id: 3, cat:'pc', price:32, name:'Headset Gamer Pro H2', desc:'Sonido envolvente y micro flexible.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/1bba8861c-1ee8-4326-9ada-4f14a96ac994.png'
}

,
{
id: 4, cat:'pc', price:27, name:'Webcam Full HD 1080p', desc:'Con micrófono integrado.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/19e60117a-b5e9-45fb-bb9a-9c00cfd2a142.png'
}

,
{
id: 5, cat:'tlf', price:15, name:'Cargador Rápido 25W USB‑C', desc:'Carga rápida PD multimarca.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/1f1634d8e-9c80-463e-b685-361b7114b879.png'
}

,
{
id: 6, cat:'tlf', price:8, name:'Cable Trenzado USB‑C 2 m', desc:'Nylon reforzado y conectores de aluminio.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/102702188-0d43-46fa-9b2b-de888ac7db64.png'
}

,
{
id: 7, cat:'tlf', price:22, name:'Power Bank 10.000 mAh', desc:'Doble salida con pantalla digital.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/15d36eeab-196d-4673-98ff-9fb25f94a230.png'
}

,
{
id: 8, cat:'tlf', price:18, name:'Earbuds Bluetooth TWS', desc:'Control táctil y estuche de carga.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/1ccaad71a-947d-4d26-84e6-8cc209c288b5.png'
}

,
{
id: 9, cat:'belleza', price:35, name:'Deep Vita C Capsule Cream', desc:'Vitamina C + Niacinamida 5% · 55 g.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/116f6a03c-819a-41b6-aa3b-946b214a318f.png'
}

,
{
id: 10, cat:'belleza', price:20, name:'Ácido Azelaico 10%', desc:'Fórmula iluminadora · 30 ml.', img:'https://image.qwenlm.ai/public_source/1310e334-0345-45b6-afbb-14b1c138cbad/1250c8c9f-9605-443b-821b-f454cb109948.png'
}

];

let currentCat='all';

let cart= {}

;

try {
    cart=JSON.parse(localStorage.getItem('tcellCart'))|| {}
}

catch(e) {
    cart= {}
}

const $=id=>document.getElementById(id);
const gridEl=$('grid'),
badgeEl=$('cartCount'),
itemsEl=$('cartItems'),
totalEl=$('cartTotal'),
drawerEl=$('drawer'),
overlayEl=$('overlay'),
toastEl=$('toast'),
searchEl=$('search');
const money=n=>'$'+n.toFixed(2);

function renderProducts() {
    const q=searchEl.value.trim().toLowerCase();
    const list=PRODUCTS.filter(p=>(currentCat==='all' ||p.cat===currentCat)&&(p.name.toLowerCase().includes(q)||p.desc.toLowerCase().includes(q)));

    if( !list.length) {
        gridEl.innerHTML='<p class="empty">No se encontraron resultados.</p>';
        return;
    }

    gridEl.innerHTML=list.map((p, i)=>` <article class="card" style="animation-delay:${i*0.05}s" > <div class="ph" ><img src="${p.img}" alt="${p.name}" loading="lazy" ></div> <span class="cat" >$ {
            CATLABEL[p.cat]
        }

        </span> <h3>$ {
            p.name
        }

        </h3> <p class="desc" >$ {
            p.desc
        }

        </p> <p class="price" >$ {
            money(p.price)
        }

        <span>USD</span></p> <button class="addB" onclick="addToCart(${p.id})" >Añadir a la bolsa</button> </article>`).join('');
}

function setCat(cat) {
    currentCat=cat;
    document.querySelectorAll('#chips button').forEach(b=>b.classList.toggle('active', b.dataset.cat===cat));
    renderProducts();
}

function goCat(cat) {
    setCat(cat);
}

$('chips').addEventListener('click', e=> {
        const b=e.target.closest('button'); if(b)setCat(b.dataset.cat);
    });
searchEl.addEventListener('input', renderProducts);

function saveCart() {
    localStorage.setItem('tcellCart', JSON.stringify(cart));
}

function addToCart(id) {
    cart[id]=(cart[id]||0)+1;
    saveCart();
    syncCart();
    showToast('Añadido a la bolsa');
}

function changeQty(id, d) {
    cart[id]=(cart[id]||0)+d;
    if(cart[id]<=0)delete cart[id];
    saveCart();
    syncCart();
}

function removeLine(id) {
    delete cart[id];
    saveCart();
    syncCart();
}

function entries() {
    return Object.entries(cart).map(([id, qty])=>({
            prod:PRODUCTS.find(p=>p.id===Number(id)), qty
        })).filter(e=>e.prod);
}

function syncCart() {
    const es=entries(),
    count=es.reduce((s, e)=>s+e.qty, 0),
    total=es.reduce((s, e)=>s+e.qty*e.prod.price, 0);
    badgeEl.textContent=count;
    badgeEl.classList.remove('pop');
    void badgeEl.offsetWidth;
    badgeEl.classList.add('pop');
    totalEl.textContent=money(total);

    if( !es.length) {
        itemsEl.innerHTML='<div class="dEmpty"><span class="ico">🛍️</span><p>Tu bolsa está vacía.</p><button class="btnP" onclick="closeDrawer()">Ver catálogo</button></div>';
        return;
    }

    itemsEl.innerHTML=es.map(({
            prod, qty

        })=>` <div class="cItem" > <img src="${prod.img}" alt="${prod.name}" > <div class="cInfo" > <h4>$ {
        prod.name
    }

    </h4> <p class="cp" >$ {
        money(prod.price)
    }

    </p> <div class="stepper" > <button onclick="changeQty(${prod.id},-1)" aria-label="Menos" >−</button> <span>$ {
        qty
    }

    </span> <button onclick="changeQty(${prod.id},1)" aria-label="Más" >+</button> </div> </div> <div class="cRight" > <strong>$ {
        money(prod.price*qty)
    }

    </strong> <button class="del" onclick="removeLine(${prod.id})" >Eliminar</button> </div> </div>`).join('');
}

$('checkout').addEventListener('click', ()=> {
        const es=entries();

        if( !es.length) {
            showToast('Tu bolsa está vacía'); return;
        }

        const lines=['🛍️ *NUEVO PEDIDO — TECNOCELL*', '──────────────────'];
        let total=0;

        es.forEach(({
                prod, qty

            })=> {
            const sub=prod.price*qty; total+=sub; lines.push('▪ ' +prod.name+'  x' +qty+'  →  ' +money(sub));
        });
    lines.push('──────────────────', '*TOTAL: ' +money(total)+'*', '', 'Quedo atento para coordinar pago y envío 🙂');
    window.open('https://wa.me/' +WHATS+'?text=' +encodeURIComponent(lines.join('\n')), '_blank');
});

function openDrawer() {
    drawerEl.classList.add('show');
    overlayEl.classList.add('show');
    document.body.style.overflow='hidden';
}

function closeDrawer() {
    drawerEl.classList.remove('show');
    overlayEl.classList.remove('show');
    document.body.style.overflow='';
}

$('openCart').addEventListener('click', openDrawer);
$('closeCart').addEventListener('click', closeDrawer);
overlayEl.addEventListener('click', closeDrawer);

document.addEventListener('keydown', e=> {
        if(e.key==='Escape')closeDrawer();
    });

let toastT=null;

function showToast(msg) {
    toastEl.textContent=msg;
    toastEl.classList.add('show');
    clearTimeout(toastT);
    toastT=setTimeout(()=>toastEl.classList.remove('show'), 2200);
}

const io=new IntersectionObserver(es=>es.forEach(e=> {
            if(e.isIntersecting) {
                e.target.classList.add('on'); io.unobserve(e.target);
            }

        }), {
    threshold:.12
});
document.querySelectorAll('.rv').forEach(el=>io.observe(el));

renderProducts();
syncCart();
</script></body></html>